spring:
  application:
    name: soongsilcarrieslove

  jpa:
    show-sql: true # sql ???? ??? ???? ??.
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/soongsilcarrieslove # mysql ?? ?? (?? ?? DB? ?? ? ??)
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: scl
    password: Scl1234! # db ????